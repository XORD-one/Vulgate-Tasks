type UserBalance @entity {
  id: ID!
  # count: BigInt!
  # from: Bytes! # address
  # to: Bytes! # address
  # value: BigInt!
  # senderBalance: BigInt!
}
type Sender @entity {
  id: ID!
  user: Bytes! # address
  senderBalance: BigInt!
}
